server:
  port: 8095
  
ms:
  inventarioBuscar:
    baseUri: "lb://MSInventario"
    uriPath: "/api/v1/productos/stockprice/{id}"
  inventarioActualizar:
    baseUri: "lb://MSInventario"
    uriPath: "/api/v1/productos/actualizar/{id}"
  auth:
    baseUri: "lb://MSAuthentication"
    uriPath: "/api/auth/consuluser/{id}"
lambda:
    email:
      cron-expression: "0 */6 * * * *" # tiempo en minutos para enviar el email de carrito abandonado
      url: "https://5b91j031fi.execute-api.us-east-1.amazonaws.com/dev/send-email"