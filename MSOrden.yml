server:
  port: 8095
  
external:
  services:
    inventario:
      baseUrl: "lb://MSInventario"
      consultar: "/api/v1/productos/stockprice/{id}"
      actualizar: "/api/v1/productos/actualizar/{id}"
    auth:
      baseUrl: "lb://MSAuthentication"
      consultar: "/api/auth/consuluser/{id}"
lambda:
    email:
      url: "https://5b91j031fi.execute-api.us-east-1.amazonaws.com/dev/send-email"